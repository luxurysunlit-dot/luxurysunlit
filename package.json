{
  "name": "luxury-sunlit",
  "version": "1.0.0",
  "description": "Premium eCommerce platform for Luxury Sunlit brand",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shopify-integration"
  ],
  "scripts": {
    "dev": "yarn workspaces run dev",
    "build": "yarn workspaces run build",
    "start": "yarn workspaces run start",
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
